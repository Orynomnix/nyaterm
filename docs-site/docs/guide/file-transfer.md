---
sidebar_position: 2
---

# SFTP 文件传输

Dragonfly 的远程文件能力建立在 SSH 会话之上。也就是说，**文件浏览器、SFTP 传输、远程文件本地编辑后回传** 都只在 SSH 会话中可用；本地终端、Telnet、串口不会显示这一套能力。

## 文件浏览器

连接 SSH 会话后，文件浏览器面板可以直接浏览远程目录。

你会得到这些基础能力：

- 自动进入远端用户主目录
- 进入文件夹、返回上级、手动输入路径跳转
- 刷新当前目录
- 与终端当前工作目录同步
- 在不支持路径跟踪的会话中关闭自动同步

## 常见文件操作

在文件列表或右键菜单中，你可以执行：

| 操作 | 说明 |
|------|------|
| 打开 | 下载到本地临时目录后，用默认编辑器打开 |
| 上传文件 | 选择本地文件上传到当前目录 |
| 上传文件夹 | 整个目录上传到当前远端路径 |
| 下载 | 下载单个文件或整个目录 |
| 重命名 | 修改远端名称 |
| 移动 | 把文件或目录移动到新路径 |
| 删除 | 删除文件或目录 |
| 属性 | 查看大小、时间、UID/GID、权限等信息 |
| 新建文件 / 文件夹 / 符号链接 | 在当前目录中直接创建 |

其中“打开”并不只是只读预览，而是为后续本地修改后回传做准备。

## 上传与下载

### 上传

可通过工具栏或右键菜单把本地文件上传到当前目录。

- 多个文件会逐个加入传输流程
- 上传目录时会保留目录结构
- 对 SSH 场景适合拿来同步脚本、配置文件或发布包

### 下载

下载时有两种常见工作方式：

- 使用默认下载目录，直接保存到固定位置
- 每次都询问保存位置，适合临时排障或按任务分类归档

目录下载与文件下载都支持，远端目录会落到你选择的本地目标目录中。

## 传输面板与传输设置

Dragonfly 会把上传 / 下载任务统一放进传输队列中，便于你查看：

- 当前进度
- 成功与失败状态
- 同时进行中的任务

在设置中还可以按需调整传输相关策略，例如：

- 上传 / 下载线程数
- 冲突时的处理策略
- 是否保留时间戳
- 是否继续断点传输
- 默认下载路径
- 默认打开远程文件所使用的本地编辑器

## 与终端路径同步

文件浏览器可以和当前 SSH 终端的工作目录配合使用：

- **手动同步**：把浏览器直接跳到终端当前路径
- **自动同步**：终端 `cd` 到新目录后，文件浏览器自动跟随

这对边跑命令边查文件非常有用，例如你在终端里进入某个发布目录后，文件浏览器会更容易保持在同一位置。

## 本地编辑后自动回传

这是 Dragonfly 很适合文档截图和真实运维场景的一项能力。

### 工作方式

1. 在 SSH 文件浏览器中对远程文件执行 **打开**
2. Dragonfly 先把文件下载到本地临时目录
3. 同时启动文件监听
4. 你在本地编辑器里保存后，应用会弹出上传提示窗口

### 上传提示窗口

文件被修改后，你可以选择：

- **上传一次**
- **始终上传**
- **取消**

如果你对某个文件选择了“始终上传”，那么在**当前会话内**后续再次保存时会直接回传，不再重复询问。

### 适合的使用场景

- 修改远程配置文件
- 调整部署脚本
- 打开日志或状态文件做本地分析
- 为文档准备“编辑后回传”的演示截图

## 文件属性与权限

在 **属性** 中可以查看：

- 文件大小
- 修改时间、访问时间
- 所有者和组
- UID / GID
- 权限的八进制表示

如果你的流程需要先核对权限再替换文件，这里会比单纯依赖 `ls -l` 更直观。

:::tip 截图建议
- 推荐图片路径：`/img/docs/file-transfer/remote-file-browser.png`
- 建议连接一个 SSH 会话后，在文件浏览器中展示目录树、工具栏与右键菜单
- 另一个推荐图片路径：`/img/docs/file-transfer/auto-upload-dialog.png`
- 建议先打开一个远程文本文件，再在本地编辑器中保存一次以触发自动上传提示
:::
